PORT=3000

# Optional: if set, landing pages are generated by Claude.
# Without it, a built-in template generator is used so the app runs out of the box.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-5
